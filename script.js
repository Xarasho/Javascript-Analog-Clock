setInterval(setClock, 1000)

function setClock() {
    const currentDate = new Date()
    const secondsRatio = currentDate.getSeconds() / 60
    const minutesRatio = currentDate.getSeconds() / 60
    const HoursRatio = currentDate.getSeconds() / 60 
}